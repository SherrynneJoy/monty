#include "monty.h"

/**
 * f_nop - does nothing
 * @head: a pointer to a pointer
 * @line_number: an integer
 * Return: nothing
 */
void f_nop(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
