#include "monty.h"

/**
 * f_nop - does nothing
 * @head: a pointer to a pointer
 * @count: an integer
 * Return: nothing
 */
void f_nop(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
}
