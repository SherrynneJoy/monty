#include "monty.h"

/**
 * f_stack - prints the elements at the top
 * @head: pointer to a pointer
 * @count: an int
 * Return: nothing
 */
void f_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;

	drive.lifi = 0;
}
